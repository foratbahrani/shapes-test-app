import React from 'react';
import './ShapesMenuButton.scss';

const ShapesMenuButton = ({ title }) => {
	return <button className='shapesMenuButton'>{title}</button>;
};

export default ShapesMenuButton;
